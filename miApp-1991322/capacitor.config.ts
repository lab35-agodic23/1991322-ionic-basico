import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'miApp-1991322',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
